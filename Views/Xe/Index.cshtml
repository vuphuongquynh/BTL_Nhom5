@model IEnumerable<baitaplonPTPMQL.Models.Xe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenXe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaiXe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenXe.XeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaiXe.MaTaiXe)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaXe">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.MaXe">Xem</a> |
                <a asp-action="Delete" asp-route-id="@item.MaXe">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
